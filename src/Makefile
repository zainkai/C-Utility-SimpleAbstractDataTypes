ADTArray := ADTArray
ADTStack := ADTStack
FLAGS := -Wall -c

all: ADTArray ADTStack

ADTStack:
	gcc $(ADTStack).c $(FLAGS)
ADTArray:
	gcc $(ADTArray).c $(FLAGS)
clean:
	rm $(ADTArray).o
	rm $(ADTStack).o